{
    "name": "micropad",
    "private": true,
    "workspaces": [
        "apps/client",
        "apps/server",
        "packages/*",
        "widgets"
    ],
    "scripts": {
        "dev": "npm run build --workspace=packages/protocol && npm run build --workspace=widgets && npm run dev --workspace=apps/client & npm run dev --workspace=apps/server",
        "build": "npm run build --workspace=packages/protocol && npm run build --workspace=widgets && npm run build --workspace=apps/client && npm run build --workspace=apps/server",
        "start": "npm run start --workspace=apps/server"
    }
}
