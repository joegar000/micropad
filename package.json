{
    "name": "micropad",
    "private": true,
    "workspaces": [
        "apps/client",
        "apps/desktop",
        "packages/*",
        "widgets"
    ],
    "scripts": {
        "dev": "npm run build --workspace=packages/protocol && npm run build --workspace=widgets && npm run dev --workspace=apps/client & npm run dev --workspace=apps/desktop",
        "build": "npm run build --workspace=packages/protocol && npm run build --workspace=widgets && npm run build --workspace=apps/client && npm run build --workspace=apps/desktop",
        "start": "npm run start --workspace=apps/desktop",
        "test": "npm run test:unit",
        "test:unit": "npm run build --workspace=packages/protocol && npm run build --workspace=widgets && npm run build --workspace=apps/desktop && node --test packages/protocol/test/*.test.mjs apps/desktop/test/*.test.mjs"
    }
}
