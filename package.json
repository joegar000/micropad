{
  "name": "micropad-workspace",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "app",
    "sdk"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "start": "(cd app && npm start)"
  }
}
